---
phase: 05-paywall-coverage-expansion-upgrade-nudge
plan: 07
type: execute
wave: 4
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md
  - .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md
autonomous: false
requirements: [PAY-02, PAY-05]

must_haves:
  truths:
    - "One written document states the free/paid boundary, names every surface that enforces it, and names the follow-ups this phase deliberately did not close"
    - "A human has confirmed on a real device that each of the four newly-gated drill screens redirects, that each gate trigger shows its own paywall copy, and that the nudge and reconciliation banners behave"
    - "A free user can still reach every free-band item on a mixed-band theme deck"
  artifacts:
    - path: ".planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md"
      provides: "The coherent, explainable gating rule PAY-02 Success Criterion 1 requires, plus the enforcement-surface inventory"
      contains: "FREE_BANDS"
    - path: ".planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md"
      provides: "Per-task verification map with every status resolved"
      contains: "nyquist_compliant"
  key_links:
    - from: "05-GATING-RULE.md"
      to: "ealch-v2/src/store/entitlement.logic.ts"
      via: "the document cites the code that IS the rule, never restates it as prose that can drift"
      pattern: "entitlement\\.logic\\.ts"
---

<objective>
Close PAY-02's first success criterion — "Phase 5 planning determines and documents one coherent, explainable gating rule, and applies it consistently across the catalogue" — with a written rule that points at the code rather than duplicating it, and prove the whole phase on a real device.

Purpose: six plans changed behaviour across sixteen files. The boundary is now consistent, but nothing in the repo yet states it in one place, and none of it has been seen on a handset. Every screen-level change in this phase was verified by source-text assertion rather than by rendering, because no RNTL infrastructure exists yet (TEST-02 is a later phase). Those assertions prove the code says the right thing; only a device pass proves it does the right thing.

Output: one gating-rule document, one resolved validation map, one blocking human verification.

This plan is deliberately last and deliberately not autonomous.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-CONTEXT.md
@.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-RESEARCH.md
@.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md
@.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-01-SUMMARY.md
@.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-02-SUMMARY.md
@.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-03-SUMMARY.md
@.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-04-SUMMARY.md
@.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-05-SUMMARY.md
@.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the gating rule document and resolve the validation map</name>
  <files>.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md, .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md</files>
  <read_first>
    - ealch-v2/src/store/entitlement.logic.ts (as it stands after Plan 01 — FEATURES, PREMIERE_FEATURES, FREE_BANDS, levelLocked, drillDeckGate, roleplayLocked, roleplayNudgeDue, and the DEV_UNLOCK_A2 / a2LockLifted block at lines 168-199)
    - ealch-v2/src/utils/examGate.logic.ts (examPaperAllowed — the exam tier's client predicate, referenced by the inventory)
    - .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-CONTEXT.md (D-01 through D-17 — the document must be able to answer "which decision does this come from" for every line)
    - All six 05-0N-SUMMARY.md files produced by this phase (the enforcement inventory must match what actually shipped, not what was planned)
    - .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md (the Per-Task Verification Map is already populated with 17 rows by planning; this task resolves their Status column)
  </read_first>
  <action>
**1. Create `.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md`** with the sections below, in this order. The rule must CITE the code, never restate it as prose that can drift out of sync. Where a bracketed instruction appears, write real content and delete the instruction.

Section 1 — title and status line. Title: `# Ealch gating rule (ratified Phase 5)`. State that this is the rule of record and that it supersedes the "only 1 of 12+ eligible lessons is gated" framing in PROJECT.md and REQUIREMENTS.md, which direct code reads during Phase 5 discussion and research found to be stale: the boundary ROADMAP.md described as "one candidate raised in research" was already fully implemented.

Section 2 — `## The rule, in one sentence`. Exactly this: Sons and A1 are free forever; everything past A1, unlimited coach turns and unlimited role plays are Première; exam papers are a separate Examiner pass that Première does not include. Cite D-01 and D-02.

Section 3 — `## Where the rule lives in code`. This table, verbatim:

| Concept | Single source of truth |
|---|---|
| The free bands | `FREE_BANDS` in `ealch-v2/src/store/entitlement.logic.ts` |
| What Première grants | `PREMIERE_FEATURES` in the same file |
| The full feature vocabulary | `FEATURES` in the same file |
| "Is this band locked?" | `levelLocked` (unit-shaped surfaces) / `drillDeckGate` (deck-shaped surfaces) |
| "Is this role play locked?" | `roleplayLocked` |
| "Should the upgrade nudge fire?" | `roleplayNudgeDue` |
| "Is this exam paper allowed?" | `examPaperAllowed` (`ealch-v2/src/utils/examGate.logic.ts`) plus the server's `startExamAttempt` refusal |

Follow it with: no screen re-derives a gate from `entitlement.plan` or `entitlement.features.includes(...)`; a new surface that needs a gate calls one of the predicates above.

Section 4 — `## Enforcement inventory`. [Build this table from the six SUMMARY files. One row per surface: screen, what it gates, which predicate it calls, and whether the gate is render-time redirect, press-time route, or server-side. Cover all twelve: `den.tsx`, `lesson.tsx`, `narrated.tsx`, `placement.tsx`, `flashcards.tsx`, `dictation.tsx`, `voiceflash.tsx`, `sentence.tsx`, `chat.tsx`, `roleplay.tsx`, `exam.tsx`, `exam-paper.tsx`. Mark the four drill screens as new in Phase 5. Note explicitly that `chat.tsx` is correctly gated even though a grep for `useFeature` will not find it, because its cap is server-enforced by design.]

Section 5 — `## Why the two deck shapes need two rules`. [Explain with the actual route params why `theme.tsx`'s explicit `?theme=&level=` redirects the whole screen while the four `*themes.tsx` screens' `?theme=`-only decks are filtered item by item, and why `flashcards.tsx`'s `?domain=` and `?deck=new` shapes and `sentence.tsx`'s no-theme `introEligible` path fall under the same filter. Cite `drillDeckGate`'s three-case contract and its deliberate "empty raw deck is not a paywall" behaviour.]

Section 6 — `## Visibility versus enforcement`. [State that the PREMIÈRE pill on `den.tsx`, `theme.tsx` and the four `*themes.tsx` screens is visibility only, that locked rows stay pressable by design, and that the destination screen is the chokepoint. Quote `den.tsx`'s own comment: locked units stay fully visible, they just route to the paywall instead of the lesson. Cite UI-SPEC Copywriting item 6.]

Section 7 — `## What the paywall says, and when`. [One row per `from` value: `gate:levels`, `gate:coach`, `gate:roleplay`, `gate:examiner`, and the generic fallback for `home`/`settings`/`placement`/direct. State that `gate:examiner` is a distinct render branch with no plan picker and no purchase action, and why: `'examiner'` is not in `PREMIERE_FEATURES`, so selling Première under an exam headline would take money for something that does not unlock what the user tried to open. Cite D-08 and UI-SPEC Copywriting item 2.]

Section 8 — `## What this rule is NOT`:
- **Not a security boundary.** The corpus ships bundled in the app binary, so every client-side band check is bypassable by inspecting the bundle. The real access-control boundaries are server-side and were not touched by this phase: the coach turn cap (Phase 3 `quota.ts`) and `startExamAttempt`'s `needs-exam-tier` refusal. See 05-RESEARCH.md's Security Domain section.
- **Not final pricing policy.** PAY-04 ("tune the gating boundary on real conversion data") is a deferred requirement, not part of this milestone.

Section 9 — `## Known temporary state`. [Record `DEV_UNLOCK_A2` / `a2LockLifted()`: currently `false`, `__DEV__`-only, scoped to `levels.all` alone, and unable to be taken by a release build. State that `useFeature` is the ONE read that consults it, which is why every gate added in this phase goes through `useFeature` rather than a raw `hasFeature` call, and why `drillDeckGate` takes a `levelsAll` boolean rather than an `Entitlement`.]

Section 10 — `## Deliberate follow-ups (named, not dropped)`:

| Item | Why it is not in Phase 5 | Where it goes |
|---|---|---|
| Coach-cap upgrade nudge | No client-side remaining-turn signal exists; `coach.ask()` returns `{ reply, live, capped }` only, and the cap is server-enforced. A client-side counter would drift from Phase 3's `quota.ts` the moment either changed. `BannerState` already reserves `trigger: 'coach'`, so adding it later needs no type change. | A phase that touches the coach edge function |
| An exam-tier purchase surface | No purchase flow for the Examiner pass exists anywhere in the app, which is why `gate:examiner` explains and dismisses rather than selling. | Out of this milestone's monetisation scope |
| `audio.packs` | Listed in `FEATURES` and granted by `PREMIERE_FEATURES`, but no packs exist yet and nothing markets it on the paywall. Verified in Phase 5: `featureRows` has exactly three entries, so CONTEXT.md's D-03 removal was already satisfied and closed by verification rather than by an edit. | Phase 7 |
| RNTL component tests for the gated screens | No render-test infrastructure exists yet; Phase 5's screen-level guarantees are source-text assertions plus a device pass. | TEST-02 |
| PAY-04 boundary tuning | Requires real conversion data, which requires PAY-02 to be live first. | Deferred requirement |

**2. Resolve `.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md`.** Planning already populated the Per-Task Verification Map with all 17 rows (`05-01-T1` through `05-07-T2`), their threat refs and their automated commands. This task only:
- Changes each row's **Status** from `pending` to `green` / `red` / `flaky` based on what actually ran.
- Corrects any row whose **Automated Command** or **File Exists** value changed during execution, and says so.
- Adds a row for any task that was split or added during execution, keeping the `05-PP-TN` id convention.
- Updates the **Measured runtime** line with the wall clock of the post-phase `npm --prefix ealch-v2 test` run, and updates **Max feedback latency** if it moved.
- Flips `wave_0_complete: true` in the frontmatter (all four gaps are closed once the suites exist and pass), and sets `status: complete`.
- Leaves `nyquist_compliant: true` only if every row in the map is `green`; if any is `red` or `flaky`, set it `false` and name the row in the Approval line.
- Ticks the last Validation Sign-Off checkbox.
  </action>
  <verify>
    <automated>npm --prefix ealch-v2 test && test -f .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md && grep -c "FREE_BANDS" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md` exists.
    - `grep -c "FREE_BANDS" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md` returns at least `1`.
    - `grep -c "entitlement.logic.ts" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md` returns at least `2`.
    - `grep -c "^\[" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md` returns `0` (no bracketed authoring instruction was shipped).
    - `grep -c "Not a security boundary" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md` returns `1`.
    - `grep -c "Coach-cap upgrade nudge" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md` returns `1` (the deferred item is named, not dropped).
    - The enforcement inventory names all twelve screens: `grep -c "flashcards.tsx\|dictation.tsx\|voiceflash.tsx\|sentence.tsx\|den.tsx\|lesson.tsx\|narrated.tsx\|placement.tsx\|chat.tsx\|roleplay.tsx\|exam.tsx\|exam-paper.tsx" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md` returns at least `12`.
    - `grep -c "^| 05-0" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md` returns at least `17`.
    - `grep -c "| pending |" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md` returns `0` (every status resolved).
    - `grep -c "wave_0_complete: true" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md` returns `1`.
    - `grep -c "status: complete" .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-VALIDATION.md` returns `1`.
    - No em dash character appears in the new document (project standing rule): `grep -c $'—' .planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-GATING-RULE.md` returns `0`.
    - `npm --prefix ealch-v2 test` exits 0.
  </acceptance_criteria>
  <done>The gating rule is written, cites code rather than paraphrasing it, inventories all twelve enforcement surfaces, states plainly that it is not a security boundary, and names all five deferred follow-ups. Every row of the validation map has a resolved status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device verification of the whole phase</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/04-entitlement-verification-signed-out-purchase-fix/04-08-SUMMARY.md (the device-verification method Phase 4 used, including how it forced an entitlement transition)
    - ealch-v2/src/store/entitlement.logic.ts (the DEV_UNLOCK_A2 constant at line ~193 — this must read false or every gate item is a false pass)
    - All six 05-0N-SUMMARY.md files (so any device failure can be attributed to the owning plan)
  </read_first>
  <action>
Write no code in this task. Run the three preconditions, present the 21-item checklist below to the developer, wait for their report, and record the outcome.

Preconditions to run first, with their output included in the message to the developer:
1. `grep -n "const DEV_UNLOCK_A2" ealch-v2/src/store/entitlement.logic.ts` — must read `= false`. If it reads `true`, stop and report; every gate item in section A would produce a false pass.
2. `npm --prefix ealch-v2 test` — must exit 0.
3. `adb devices` — confirm exactly one device is attached.

Then ask the developer to walk items A through G and report per item. If any item fails, record the item number, what was observed, and which plan owns the file, in this plan's SUMMARY. Do not attempt a fix inside this checkpoint.
  </action>
  <what-built>
Six plans, sixteen files:
- **Plan 01** — `drillDeckGate` / `roleplayNudgeDue` predicates, 12 new FR+EN strings, 3 new analytics events.
- **Plan 02** — the `setResume` leak fixed in `lesson.tsx` and `narrated.tsx`; the PREMIÈRE lock pill added to `theme.tsx` and the four `*themes.tsx` browse screens.
- **Plan 03** — band gates added to `flashcards.tsx`, `dictation.tsx`, `voiceflash.tsx`, `sentence.tsx`, which had none at all.
- **Plan 04** — contextual paywall copy keyed on `from`; a dismiss-only explainer branch for `gate:examiner`; `from: 'gate:examiner'` added to all three exam gates.
- **Plan 05** — `useUI`'s banner slot generalised to a tagged union; `PushBanner` made kind-driven with a 10s auto-hide for the new kinds.
- **Plan 06** — the roleplay upgrade nudge (24h cadence) and the reconciliation notice on a genuine downgrade.

Everything above was verified by source-text assertion, because no RNTL infrastructure exists yet. This checkpoint is the only render-level proof in the phase.
  </what-built>
  <how-to-verify>
Run on a free account. Deep links use the `ealch` scheme (not `wonerock`), one link at a time:
```
adb shell am start -W -a android.intent.action.VIEW -d "ealch://flashcards?theme=<a2-theme>&level=a2"
```

**A. The four newly-gated drill screens (PAY-02).** Signed in as a free user, deep-link each of these and confirm every one lands on the paywall with the headline "Unlock A2 and beyond", with **no** flash of deck content on the way:
1. `ealch://flashcards?theme=<a2-theme>&level=a2`
2. `ealch://dictation?theme=<a2-theme>&level=a2`
3. `ealch://voiceflash?theme=<a2-theme>&level=a2`
4. `ealch://sentence?theme=<a2-theme>&level=a2`

**B. The false-positive check — the one that matters most.** Confirm the gate did NOT wall off free content:
5. Open a theme whose level range spans A1 to B1 from `/flashthemes` (tap the row, so no `level` param is sent). The deck must open and must contain only sons/A1 cards.
6. Open `/sentence` with no theme, from its normal entry point. It must open a deck, not the paywall.
7. Open a sons or A1 parcours step from `theme.tsx`. It must open normally.

**C. Lock visibility (PAY-02).**
8. In `theme.tsx`, select the A2 level chip. Every parcours step row shows the PREMIÈRE pill, and tapping one still routes (to the paywall) rather than being inert.
9. On `/flashthemes`, `/dictationthemes`, `/voicethemes`, `/sentencethemes`: rows whose lowest band is A2+ show the pill; rows that include A1 do not.

**D. The resume leak (D-05).**
10. Tap a locked A2 lesson from the Den, get bounced to the paywall, close it, and go to home. The "continue" hero must NOT name that A2 lesson.

**E. Contextual paywall copy (D-08, D-02).**
11. From a gated lesson: headline "Unlock A2 and beyond".
12. From the coach cap in `/chat`: headline "Unlimited coach conversations".
13. From a second role play the same day: headline "Unlimited role plays".
14. From an exam paper in `/exam`: the **explainer** screen — lock icon in a neutral grey badge, "Exam papers are a separate pass", a "Got it" button, and **no plan picker and no price anywhere on the screen**.
15. From Settings or home: today's generic "Unlock the whole path" screen, unchanged.
16. Close each of the above with the X. You return where you were, and the content is still locked.

**F. The banner slot (PAY-05, ROADMAP criterion 5).**
17. Settings → Test alarm. The Speak Mode banner appears with the time in its top-right corner, and tapping it opens Speak Mode. This must be exactly as it was before this phase.
18. Complete one role play on a free account. The upgrade nudge appears with the EALCH PREMIÈRE masthead, no top-right time, and auto-hides after about 10 seconds. Tapping it (before it hides) opens the paywall with the "Unlimited role plays" headline.
19. Complete a second role play the same day. The nudge must NOT reappear.

**G. Reconciliation (PAY-02 criterion 4).**
20. Force a genuine downgrade using the method recorded in `04-08-SUMMARY.md`. The banner appears with the ACCOUNT masthead in a **neutral grey** badge (not accent), reading "Your Première access has ended. Tap to check your subscription." Tapping it opens Settings, where Restore Purchases lives.
21. Force-stop the app, put the device in airplane mode, and cold-start it as a paying user. The reconciliation banner must NOT appear. (This is Phase 4's offline false-positive guard; Plan 06 must not have broken it.)

Report per item: pass, or what you saw instead.
  </how-to-verify>
  <verify>
    <automated>npm --prefix ealch-v2 test && grep -n "const DEV_UNLOCK_A2 = false" ealch-v2/src/store/entitlement.logic.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const DEV_UNLOCK_A2 = false" ealch-v2/src/store/entitlement.logic.ts` returns `1` before the pass begins.
    - `npm --prefix ealch-v2 test` exits 0 before the pass begins.
    - `adb devices` lists exactly one attached device.
    - The developer has reported an outcome for all 21 items.
    - Every failure is recorded in `05-07-SUMMARY.md` with its item number, the observed behaviour, and the owning plan number.
  </acceptance_criteria>
  <done>All 21 items reported. Either the developer typed "approved", or every failing item is recorded with its owning plan so a gap-closure plan can be written.</done>
  <resume-signal>Type "approved" to close the phase, or list the item numbers that failed with what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future implementers | The gating rule document is read by later phases and by cross-AI reviewers; a rule that paraphrases code instead of citing it drifts silently. |
| device under test → verification claim | A dev build with the A2 unlock flipped would produce a false pass on every item in section A. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-34 | Tampering | `DEV_UNLOCK_A2` accidentally `true` during the device pass | mitigate | Task 2's action runs `grep -n "const DEV_UNLOCK_A2" ealch-v2/src/store/entitlement.logic.ts` as a precondition and stops if it is not `false`. The flag is `__DEV__`-guarded and scoped to `levels.all` alone, so a release build cannot take it regardless. |
| T-05-35 | Repudiation | A phase whose screen-level behaviour was only ever asserted as source text | mitigate | This checkpoint is the render-level evidence, and it is `gate="blocking"` on purpose. The four drill-screen gates, the four paywall branches and the three banner kinds have no automated render coverage until TEST-02 builds RNTL, and that gap is recorded in the gating-rule document's follow-up table rather than left implicit. |
| T-05-36 | Information disclosure | Documenting the gating rule as if it were a security control | mitigate | Section 8 of the document states in its own heading that this is not a security boundary, names the corpus-in-bundle reason, and names the two server-side boundaries (`quota.ts`, `startExamAttempt`) that are the real controls. An acceptance criterion greps for that heading, so it cannot be quietly dropped. |
| T-05-37 | Elevation of privilege | A false-negative gate walling a paying user out of content they bought | mitigate | Section B of the device pass exists specifically for the inverse failure: a mixed-band deck, a no-theme deck and a free-band step must all still open. A gate that over-blocks is a refund event, and it is the failure mode the source-text assertions cannot catch. |
| T-05-38 | Spoofing | Deep links used for verification granting access the normal flow would not | accept | The deep links used here are the same routes the app's own navigation pushes, with the same params. If a deep link reached gated content that the in-app path blocked, that is a finding the checkpoint is designed to surface, not a flaw in the method. |
</threat_model>

<verification>
1. `npm --prefix ealch-v2 test` — the full suite, including all five wiring suites and the three entitlement suites, green.
2. `npm --prefix ealch-v2 run typecheck` — clean.
3. `npm --prefix ealch-v2 run test:i18n` — FR/EN parity holds with the 12 new keys.
4. `grep -rn "bannerVisible\|bannerAt" ealch-v2/src ealch-v2/app` — no matches.
5. Task 2's 21 device items, all reported pass by the developer.
</verification>

<success_criteria>
- `05-GATING-RULE.md` exists, states the rule in one sentence, cites `entitlement.logic.ts` as the source of truth rather than paraphrasing it, inventories all twelve enforcement surfaces, explains the two deck shapes, separates visibility from enforcement, states explicitly that this is not a security boundary, and names all five deferred follow-ups including the coach-cap nudge.
- `05-VALIDATION.md` has every row's status resolved, a measured runtime, `wave_0_complete: true`, and `status: complete`.
- The developer has approved the 21-item device pass, or every failure is recorded and a follow-up plan exists.
- PAY-02 and PAY-05 are both evidenced: PAY-02 by the rule document plus items A, B, C, D, E and G; PAY-05 by items F18 and F19.
</success_criteria>

<output>
After completion, create `.planning/phases/05-paywall-coverage-expansion-upgrade-nudge/05-07-SUMMARY.md`
</output>
