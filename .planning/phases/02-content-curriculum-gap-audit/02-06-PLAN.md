---
phase: 02-content-curriculum-gap-audit
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/02-content-curriculum-gap-audit/GAPS.md
autonomous: false
requirements: [CONTENT-01]

must_haves:
  truths:
    - "Every GAPS.md finding flagged phase-worthy has a corresponding stub phase in ROADMAP.md, added in this phase rather than deferred to a later triage session"
    - "GAPS.md ends with a closing statement naming exactly what was checked, in the language ROADMAP Success Criterion 3 requires"
    - "CONTENT-01's checkbox and traceability row in REQUIREMENTS.md reflect the audit's real outcome, so Phase 1's left-behind bookkeeping gap is not repeated"
    - "The developer has seen and approved GAPS.md and any roadmap change before the phase closes"
  artifacts:
    - path: ".planning/phases/02-content-curriculum-gap-audit/GAPS.md"
      provides: "Closing statement for CONTENT-01 (ROADMAP Success Criterion 3)"
      contains: "## Gaps Summary"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CONTENT-01 traceability closed"
      contains: "CONTENT-01"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/02-content-curriculum-gap-audit/GAPS.md"
      via: "each stub phase's Requirements/Goal line citing the GAP-nn it closes"
      pattern: "GAP-[0-9]"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/02-content-curriculum-gap-audit/GAPS.md"
      via: "CONTENT-01 traceability row status set from the audit outcome"
      pattern: "CONTENT-01"
---

<objective>
Turn the audit's output into tracked work: insert a ROADMAP stub phase for every
phase-worthy finding (D-07), write CONTENT-01's closing statement, close its
traceability row in REQUIREMENTS.md, and get developer sign-off.

Purpose: D-07 is explicit that phase-worthy findings get a roadmap phase
IMMEDIATELY, not at a later triage session — an audit whose output sits unlinked
in a findings file has not actually closed anything. And PATTERNS.md records that
Phase 1 finished functionally complete while leaving its PUBLISH-01/02
traceability rows on "Pending"; that bookkeeping gap is not repeated here.

Output: updated `.planning/ROADMAP.md` (conditional), updated
`.planning/REQUIREMENTS.md`, updated `.planning/STATE.md` (conditional), and
GAPS.md's `## Gaps Summary` closing statement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-curriculum-gap-audit/02-CONTEXT.md
@.planning/phases/02-content-curriculum-gap-audit/02-PATTERNS.md
@.planning/phases/02-content-curriculum-gap-audit/GAPS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<current_state>
<!-- Read verbatim on 2026-09-19. Line numbers will shift once you edit; find by content. -->

.planning/ROADMAP.md
  line 7    : "Full completion means all 19 phases land before public submission"
  lines 11-15: Phase Numbering convention —
               "Integer phases (1, 2, 3): Planned milestone work"
               "Decimal phases (2.1, 2.2): Urgent insertions (marked with INSERTED)"
  lines 17   : the 2026-09-19 revision note explaining the Phase 11 split used
               INTEGER renumbering, not decimals, because the split was
               "neither urgent nor an insertion"
  lines 19-37: the phase checklist, Phase 1 (done) through Phase 19
  lines 39+  : "## Phase Details", one "### Phase N: <name>" block each with
               **Goal** / **Depends on** / **Requirements** / **Success Criteria** / **Plans**
  line 66-74 : "### Phase 2: Content & Curriculum Gap Audit" — Goal, Depends on: Nothing,
               Requirements: CONTENT-01, three Success Criteria, "**Plans**: TBD"
  line 304   : the trailing dated revision note, format:
               "*Roadmap revised: 2026-09-19 — <changelog>. See ROADMAP REVISED return for full changelog.*"

.planning/REQUIREMENTS.md
  line 58 : "- [ ] **CONTENT-01**: An early phase audits the current A1/A2/exam content …
             (scope discovery — no gaps are pre-assumed; if none are found, this
             requirement closes as \"audited, no gaps\")"
  line 114: "| CONTENT-01 | Phase 2 | Pending |"
  lines 142-146: the Coverage block — "v1 requirements: 29 total", "Mapped to phases: 29",
             "Unmapped: 0 ✓"
  line 150: the trailing "*Last updated: …*" line
  Precedent for a closed requirement: lines 12-13 use "- [x]" and lines 112-113
  use "| PUBLISH-01 | Phase 1 | Complete |"

.planning/STATE.md
  frontmatter: "progress:" block with "total_phases: 19"

ROADMAP Phase 2 Success Criterion 3 (the exact wording this plan must satisfy):
  "If no gaps are found, a written closing statement exists citing exactly what
   was checked, and CONTENT-01 closes as 'audited, no gaps.'"
</current_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert a ROADMAP stub phase for every phase-worthy finding (D-07)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/02-content-curriculum-gap-audit/GAPS.md (every `**Phase-worthy (D-07):**` flag — this task acts only on the ones marked `yes`)
    - .planning/ROADMAP.md (the Phase Numbering convention at lines 11-15, the 2026-09-19 revision note at line 17 showing the integer-renumber precedent, the phase checklist at lines 19-37, and a complete "### Phase N:" detail block to copy the field shape from)
    - .planning/STATE.md (the `progress:` frontmatter block — `total_phases` must stay consistent with the roadmap)
    - .planning/phases/02-content-curriculum-gap-audit/02-PATTERNS.md (lines 149-159 — the stub-phase insertion pattern and why decimal notation is wrong here)
  </read_first>
  <action>
**Act only on findings whose GAPS.md block says `**Phase-worthy (D-07):** yes`.**
D-07 is explicit that a single thin lesson or a single unlistened paper stays in
GAPS.md as a named, scoped item and does NOT get its own phase. If zero findings
are flagged `yes`, skip to Step D and record that explicitly.

**Step A — choose the number, using the documented convention.** Decimal notation
(`2.1`, `2.2`) is reserved in this roadmap for urgent post-approval insertions via
`/gsd-insert-phase` and is marked INSERTED. An audit finding is neither urgent nor
an insertion in that sense — it takes an INTEGER slot, exactly as the 2026-09-19
Phase 11 split did.

  - Default: **append** as the next integer after the current highest phase
    (currently 19, so the first new stub is Phase 20, the second Phase 21).
    Appending requires NO renumbering of anything.
  - Only if a stub MUST be sequenced before an existing phase (for example, a
    content gap that Phase 15's snapshot split would carry forward) do you insert
    mid-list — and then you renumber every later phase +1 in BOTH the checklist
    (lines 19-37) and every `### Phase N:` detail heading, AND update every
    REQUIREMENTS.md traceability row that names a renumbered phase. State the
    reason for choosing insertion over appending in the revision note.

**Step B — write the checklist entry**, matching the existing line shape exactly:
```markdown
- [ ] **Phase 20: <Name>** - <one-line description of the work, not of the finding>
```

**Step C — write the detail block**, matching the existing field set exactly:
```markdown
### Phase 20: <Name>
**Goal**: <outcome-shaped, what must be TRUE when this ships — not "build X">
**Depends on**: <Nothing, or the phase ids this needs>
**Requirements**: CONTENT-01 (follow-up; surfaced by Phase 2's audit as GAP-nn)
**Success Criteria** (what must be TRUE):
  1. <measurable, naming the specific unit/lesson/paper ids from the GAP-nn finding>
  2. <…>
**Plans**: TBD
**Source**: `.planning/phases/02-content-curriculum-gap-audit/GAPS.md` GAP-nn (severity <s>, <date>)
```
The Success Criteria must carry the SAME named subjects the GAP-nn finding named.
A stub phase whose criteria say "fix the content gaps" is useless; one that says
"a2.17, a2.21 and a2.24 each carry a non-placeholder canDo" is executable.

**Step D — update the counts and leave a revision note.**
  - If any phase was added: change ROADMAP.md line 7's "all 19 phases" to the new
    total, and change STATE.md's `total_phases: 19` in the `progress:` frontmatter
    block to the same new total. Leave every other STATE.md field alone — this
    plan touches `total_phases` and nothing else in that file.
  - Append a dated revision note to the END of ROADMAP.md, in the existing format
    at line 304:
    `*Roadmap revised: <date> — Phase(s) <N…> added from Phase 2's content audit (GAPS.md GAP-nn…). Appended as integer slot(s); no renumbering required.*`
    (adjust the wording if you renumbered).
  - If NO phase was added, add nothing to ROADMAP.md and nothing to STATE.md, and
    record in your task output the exact sentence: "No phase-worthy findings
    (D-07); ROADMAP.md and STATE.md unchanged."
  </action>
  <acceptance_criteria>
    - If GAPS.md contains at least one `**Phase-worthy (D-07):** yes`: ROADMAP.md contains a matching `- [ ] **Phase ` checklist line and a matching `### Phase ` detail block for each, each detail block containing the literal labels `**Goal**:`, `**Depends on**:`, `**Requirements**:`, `**Success Criteria**`, `**Plans**: TBD` and `**Source**:` with a `GAP-` reference
    - If any phase was added: ROADMAP.md no longer contains the string `all 19 phases`, and STATE.md's `total_phases:` value equals the new highest phase number
    - If no phase was added: `git diff --stat .planning/ROADMAP.md .planning/STATE.md` shows no changes to either file
    - ROADMAP.md contains no new decimal phase heading (no `### Phase [0-9]+\.[0-9]`) — audit findings take integer slots
    - Every `### Phase ` heading number in ROADMAP.md is unique (no duplicate phase numbers introduced)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'R=.planning/ROADMAP.md; S=.planning/STATE.md; G=.planning/phases/02-content-curriculum-gap-audit/GAPS.md; pw=$(grep -c "\*\*Phase-worthy (D-07):\*\* yes" "$G" || true); echo "phase-worthy findings = $pw"; dups=$(grep -oE "^### Phase [0-9]+" "$R" | sort | uniq -d); test -z "$dups" || { echo "DUPLICATE phase numbers: $dups"; exit 1; }; grep -qE "^### Phase [0-9]+\.[0-9]" "$R" && { echo "FORBIDDEN decimal phase added"; exit 1; }; if [ "$pw" -gt 0 ]; then grep -qE "\*\*Source\*\*:.*GAP-[0-9]+" "$R" || { echo "stub phase missing GAP- source citation"; exit 1; }; grep -qF "all 19 phases" "$R" && { echo "phase count in overview not updated"; exit 1; }; hi=$(grep -oE "^### Phase [0-9]+" "$R" | grep -oE "[0-9]+" | sort -n | tail -1); tp=$(grep -oE "total_phases: [0-9]+" "$S" | grep -oE "[0-9]+"); echo "highest roadmap phase=$hi state total_phases=$tp"; test "$hi" = "$tp" || { echo "STATE.md total_phases out of sync"; exit 1; }; else git diff --quiet -- "$R" "$S" || { echo "no phase-worthy findings but ROADMAP/STATE were modified"; exit 1; }; fi; echo OK'</automated>
  </verify>
  <done>Every phase-worthy finding has an executable stub phase in an integer slot with named success criteria and a GAP-nn source citation, and the phase counts in ROADMAP.md and STATE.md agree — or nothing was touched because nothing qualified.</done>
</task>

<task type="auto">
  <name>Task 2: Write the CONTENT-01 closing statement and close its traceability row</name>
  <files>.planning/phases/02-content-curriculum-gap-audit/GAPS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/02-content-curriculum-gap-audit/GAPS.md (the Baseline Census numbers, the Finding Index, the Findings section, and the `_(populated by Plan 06)_` placeholder in `## Gaps Summary`)
    - .planning/REQUIREMENTS.md (line 58's CONTENT-01 checkbox, line 114's traceability row, and lines 12-13 / 112-113 for the PUBLISH-01/02 "- [x]" and "| … | Phase 1 | Complete |" precedent to copy)
    - .planning/ROADMAP.md (the Phase 2 block at lines 66-74 — Success Criterion 3's exact wording, and the `**Plans**: TBD` line that now has a real plan count)
    - .planning/phases/01-content-publish-drift-guard-extension/01-VERIFICATION.md (the "Gaps Summary" closing-statement shape at lines 82-84, and line 36's self-flagged bookkeeping gap this task exists to avoid repeating)
  </read_first>
  <action>
**Step A — write `## Gaps Summary` in GAPS.md**, replacing `_(populated by Plan 06)_`.
ROADMAP Success Criterion 3 requires a written closing statement "citing exactly
what was checked". Cite the census numbers, not adjectives:

```markdown
## Gaps Summary

**CONTENT-01 outcome:** <audited, no gaps | audited, <n> gap(s) found and scoped>

### What was checked

| Surface | Checked | Method | Evidence |
|---------|---------|--------|----------|
| Curriculum spine — unit existence | <n>/<n> units, by id | direct SELECT on `content_units` diffed against the id set declared in `author-full-curriculum-spine.ts` | `02-EVIDENCE-spine.md` §1 |
| Curriculum spine — pedagogical fields | <n>/<n> units | `canDo` / `themes` / `prereqUnitIds` read per row; themes joined to `content_themes`, prereqs joined to the live unit set | `02-EVIDENCE-spine.md` §2 |
| PE@b2 remediation slot | 1 targeted check | lesson supply vs published PE task demand, plus `dueExamSkills()` routing read | `02-EVIDENCE-spine.md` §3 |
| Exam papers — section shape | <n>/<n> published papers, by id | `jsonb_array_elements(p.sections)` with every taskId resolved against `content_exam_tasks` | `02-EVIDENCE-exams.md` §1 |
| Exam papers — blueprint conformance | <n> paper × skill pairs | item/task counts diffed against STANDARD-tef-canada.md, STANDARD-tcf-canada.md, STANDARD-delf-b2.md, BLUEPRINT-delf-b2.md (DELF calibrated to sample 3) | `02-EVIDENCE-exams.md` §2 |
| Exam audio verification | 15 papers | E8-attestation question answered from the schema; DELF blanc-02..05 logged | `02-EVIDENCE-exams.md` §3 |
| Known quality defects | <n> defects | pinned tests re-run with recorded exit status, source re-read, fresh DB counts | `02-EVIDENCE-defects.md` §1-2 |
| Shipped seed cut | <n> units | `seed.json` id set diffed both ways against the declared unit set | `02-EVIDENCE-defects.md` §3 |

### What was NOT checked, and why

<Be honest and specific. Examples of legitimate entries: pedagogical QUALITY of
lesson prose beyond field presence (out of scope — this phase checks the spine,
not the teaching); B1/C1 content (out of milestone scope per REQUIREMENTS.md
"Out of Scope"); whether exam audio is correct to listen to (unanswerable from
data — see the E8 finding). If everything in scope was checked, say so.>

### Outcome

<If no gaps: the exact sentence "CONTENT-01 closes as 'audited, no gaps'." plus
one sentence naming the evidence basis.>
<If gaps: "<n> gap(s) found: GAP-01 … GAP-nn. <m> scoped as follow-up items in
this document; <k> promoted to ROADMAP stub Phase(s) <N…>. <j> further findings
were already owned by existing requirements and are listed under Already Tracked
Elsewhere (D-09).">

### Evidence discipline confirmation (D-03)

Every finding above cites a pasted SELECT result, a test exit status, or a
`path/to/file:LINE` source read. No finding rests on `corpus:probe`,
`content:parity`, a CEFR or nasal heuristic, a spine-drift test, or any guard's
exit code.
```

**Step B — close the traceability bookkeeping in REQUIREMENTS.md.** Make exactly
these edits and no others:
  1. Line 58's checkbox: `- [ ] **CONTENT-01**` → `- [x] **CONTENT-01**`
     (matching the PUBLISH-01/02 precedent at lines 12-13). Leave the requirement
     text itself byte-identical.
  2. Line 114's traceability row: `| CONTENT-01 | Phase 2 | Pending |` →
     `| CONTENT-01 | Phase 2 | Complete |` (matching lines 112-113's wording for
     PUBLISH-01/02).
  3. If Task 1 added stub phase(s), add one traceability row per new phase
     immediately after the CONTENT-01 row, in the same table format:
     `| CONTENT-01 (follow-up: GAP-nn) | Phase 20 | Pending |`
     and update the Coverage block (lines 142-146) so "Mapped to phases" still
     reconciles — state the new phase total in that block rather than leaving
     "29 across 19 phases" stale.
  4. Update the trailing `*Last updated: …*` line with today's date and a
     one-clause reason: `— CONTENT-01 closed by Phase 2's content audit`.

**Step C — update ROADMAP.md's Phase 2 block.** Change `**Plans**: TBD` under
`### Phase 2: Content & Curriculum Gap Audit` to `**Plans**: 6 plans` and add the
plan list beneath it in the shape Phase 1 uses (lines 52-63): a `**Wave N**`
heading per wave with `- [x] 02-NN-PLAN.md — <objective>` lines. Waves are
1: plan 01; 2: plans 02, 03, 04; 3: plan 05; 4: plan 06.

Do NOT mark the `- [ ] **Phase 2: …**` checklist entry at line 20 as `[x]` — that
is `/gsd-verify-work`'s call at phase close, not this plan's.
  </action>
  <acceptance_criteria>
    - GAPS.md contains no occurrence of `_(populated by Plan 06)_`
    - GAPS.md `## Gaps Summary` contains the literal headings `### What was checked`, `### What was NOT checked, and why`, `### Outcome`, `### Evidence discipline confirmation (D-03)`
    - GAPS.md's Outcome section contains either the literal string `audited, no gaps` or the literal string `gap(s) found`
    - The "What was checked" table has at least 8 data rows
    - REQUIREMENTS.md contains `- [x] **CONTENT-01**` and does NOT contain `| CONTENT-01 | Phase 2 | Pending |`
    - REQUIREMENTS.md contains `| CONTENT-01 | Phase 2 | Complete |`
    - ROADMAP.md's Phase 2 block contains `**Plans**: 6 plans` and does not contain `**Plans**: TBD` within that block
    - ROADMAP.md contains `02-01-PLAN.md`, `02-02-PLAN.md`, `02-03-PLAN.md`, `02-04-PLAN.md`, `02-05-PLAN.md`, `02-06-PLAN.md`
    - ROADMAP.md's Phase 2 checklist entry is still `- [ ] **Phase 2:` (not marked complete by this plan)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'G=.planning/phases/02-content-curriculum-gap-audit/GAPS.md; R=.planning/REQUIREMENTS.md; M=.planning/ROADMAP.md; grep -qF "_(populated by Plan 06)_" "$G" && { echo "placeholder remains"; exit 1; }; for h in "### What was checked" "### What was NOT checked, and why" "### Outcome" "### Evidence discipline confirmation (D-03)"; do grep -qF "$h" "$G" || { echo "MISSING: $h"; exit 1; }; done; grep -qE "audited, no gaps|gap\(s\) found" "$G" || { echo "no CONTENT-01 outcome statement"; exit 1; }; grep -qF "- [x] **CONTENT-01**" "$R" || { echo "CONTENT-01 checkbox not closed"; exit 1; }; grep -qF "| CONTENT-01 | Phase 2 | Pending |" "$R" && { echo "traceability row still Pending"; exit 1; }; grep -qF "| CONTENT-01 | Phase 2 | Complete |" "$R" || { echo "traceability row not set to Complete"; exit 1; }; grep -qF "**Plans**: 6 plans" "$M" || { echo "Phase 2 plan count not recorded"; exit 1; }; for p in 01 02 03 04 05 06; do grep -qF "02-$p-PLAN.md" "$M" || { echo "MISSING plan listing 02-$p"; exit 1; }; done; grep -qF "- [ ] **Phase 2:" "$M" || { echo "Phase 2 checklist entry changed — that is verify-works call"; exit 1; }; echo OK'</automated>
  </verify>
  <done>GAPS.md carries a census-cited closing statement in ROADMAP Success Criterion 3's language, CONTENT-01's checkbox and traceability row are closed, and Phase 2's plan list is recorded in the roadmap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Developer review of the audit output</name>
  <read_first>
    - .planning/phases/02-content-curriculum-gap-audit/GAPS.md (the finished document being presented)
  </read_first>
  <what-built>
Plans 01-06 produced a read-only audit of the shipped A1/A2 curriculum spine and
the 15 published exam papers, plus fresh measurements of the known content-quality
defects, all evidence-cited per D-03. No content was authored or fixed. Written
artefacts:
  - `.planning/phases/02-content-curriculum-gap-audit/GAPS.md` (the deliverable)
  - `02-EVIDENCE-spine.md`, `02-EVIDENCE-exams.md`, `02-EVIDENCE-defects.md`
  - `.planning/ROADMAP.md` — <n> stub phase(s) added, or unchanged
  - `.planning/REQUIREMENTS.md` — CONTENT-01 closed
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/02-content-curriculum-gap-audit/GAPS.md`. Read
   `## Gaps Summary` first — it states the CONTENT-01 outcome and what was checked.
2. Spot-check evidence discipline: pick any two `### GAP-` blocks and confirm each
   **Evidence** block holds a real query plus its result (or a `path:line` source
   excerpt), not a sentence like "the guard reported clean".
3. Check `## Already Tracked Elsewhere (D-09)` — confirm you agree that each listed
   item really is already owned by the phase named, and is not being quietly
   dropped.
4. If a stub phase was added: run `git diff .planning/ROADMAP.md` and confirm the
   new phase's Success Criteria name specific unit/lesson/paper ids you could act
   on, and that the phase number does not collide with an existing one.
5. Confirm nothing was fixed: `git status` should show NO changes under
   `ealch-v2/src/content/` or `ealch-admin/scripts/` from this phase.
6. Sanity-check the scope estimates on any `**Phase-worthy (D-07):** no` findings —
   these stay in GAPS.md as named items and will not get their own phase. If you
   think one of them deserves a phase, say so now.
  </how-to-verify>
  <acceptance_criteria>
    - Developer has responded with either an approval or a specific list of changes
    - `git status --porcelain ealch-v2/src/content ealch-admin/scripts` shows no modifications attributable to this phase
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'git status --porcelain ealch-v2/src/content ealch-admin/scripts | grep -E "^ ?M" | grep -vE "seed\.json|scripts/data/|apply-narration-seed|author-narration-batch|find-french-notes" && { echo "WARNING: this phase appears to have modified source files — an audit phase must not"; exit 1; }; echo "OK — no new source modifications from this phase"'</automated>
  </verify>
  <resume-signal>Type "approved" or list the specific changes you want to GAPS.md / the roadmap stubs</resume-signal>
  <done>The developer has reviewed GAPS.md and any roadmap change, and either approved or given specific revisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → git history | This plan edits ROADMAP.md, REQUIREMENTS.md and STATE.md, which every later phase reads as its source of truth. A wrong edit here misroutes future work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | database / application source | mitigate | This plan issues NO database statement and modifies NO application source. Its `files_modified` is exactly four planning documents. Task 3's automated gate fails the plan if `ealch-v2/src/content` or `ealch-admin/scripts` shows new modifications. |
| T-02-04 | Tampering | `.planning/ROADMAP.md`, `.planning/STATE.md` | mitigate | Phase numbers must stay unique and integer; `total_phases` in STATE.md must equal the highest roadmap phase number. Both are enforced by Task 1's automated gate. STATE.md edits are limited to the single `total_phases` value — no other field in that file is touched by this plan. |
| T-02-05 | Repudiation | requirement closure | mitigate | CONTENT-01 is only marked Complete alongside a closing statement that cites the census numbers and the evidence files, so the closure is re-checkable rather than asserted. |
| T-02-02 | Information Disclosure | `GAPS.md` (git-tracked) | mitigate | The closing statement carries counts and file references only. Never introduce credentials, exam answer keys, or user-table rows. |
</threat_model>

<verification>
- GAPS.md has no remaining `_(populated by …)_` placeholders anywhere.
- Every `**Phase-worthy (D-07):** yes` finding has a matching ROADMAP stub phase.
- REQUIREMENTS.md shows CONTENT-01 as `- [x]` and `Complete`.
- ROADMAP.md's Phase 2 block lists all six plans across four waves.
- No application source file was modified by this phase.
</verification>

<success_criteria>
- CONTENT-01 closes with a statement a reader can re-verify from the evidence files.
- No phase-worthy finding leaves this phase untracked.
- Phase 1's left-behind traceability bookkeeping gap is not repeated.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-curriculum-gap-audit/02-06-SUMMARY.md`
</output>
</content>
</invoke>
